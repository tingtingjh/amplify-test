#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createItemInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createItemInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Item $colStr VALUES $valStr", "SELECT * FROM Item WHERE id=$ctx.args.createItemInput.id"]
}