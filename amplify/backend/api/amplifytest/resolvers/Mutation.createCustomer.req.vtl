#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createCustomerInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createCustomerInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Customer $colStr VALUES $valStr", "SELECT * FROM Customer WHERE id=$ctx.args.createCustomerInput.id"]
}